package net.nashlegend.sourcewall.data.database.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table "MY_BASKET".
 */
public class MyBasketDao extends AbstractDao<MyBasket, Long> {

    public static final String TABLENAME = "MY_BASKET";

    /**
     * Properties of entity MyBasket.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Section = new Property(1, int.class, "section", false,
                "SECTION");
        public final static Property Type = new Property(2, int.class, "type", false, "TYPE");
        public final static Property Name = new Property(3, String.class, "name", false, "NAME");
        public final static Property Value = new Property(4, String.class, "value", false, "VALUE");
        public final static Property CategoryId = new Property(5, String.class, "categoryId", false,
                "CATEGORY_ID");
        public final static Property CategoryName = new Property(6, String.class, "categoryName",
                false, "CATEGORY_NAME");
        public final static Property Selected = new Property(7, boolean.class, "selected", false,
                "SELECTED");
        public final static Property Order = new Property(8, int.class, "order", false, "ORDER");
    }

    ;


    public MyBasketDao(DaoConfig config) {
        super(config);
    }

    public MyBasketDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /**
     * Creates the underlying database table.
     */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists ? "IF NOT EXISTS " : "";
        db.execSQL("CREATE TABLE " + constraint + "\"MY_BASKET\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"SECTION\" INTEGER NOT NULL ," + // 1: section
                "\"TYPE\" INTEGER NOT NULL ," + // 2: type
                "\"NAME\" TEXT NOT NULL ," + // 3: name
                "\"VALUE\" TEXT," + // 4: value
                "\"CATEGORY_ID\" TEXT," + // 5: categoryId
                "\"CATEGORY_NAME\" TEXT," + // 6: categoryName
                "\"SELECTED\" INTEGER NOT NULL ," + // 7: selected
                "\"ORDER\" INTEGER NOT NULL );"); // 8: order
    }

    /**
     * Drops the underlying database table.
     */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"MY_BASKET\"";
        db.execSQL(sql);
    }

    /**
     * @inheritdoc
     */
    @Override
    protected void bindValues(SQLiteStatement stmt, MyBasket entity) {
        stmt.clearBindings();

        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getSection());
        stmt.bindLong(3, entity.getType());
        stmt.bindString(4, entity.getName());

        String value = entity.getValue();
        if (value != null) {
            stmt.bindString(5, value);
        }

        String categoryId = entity.getCategoryId();
        if (categoryId != null) {
            stmt.bindString(6, categoryId);
        }

        String categoryName = entity.getCategoryName();
        if (categoryName != null) {
            stmt.bindString(7, categoryName);
        }
        stmt.bindLong(8, entity.getSelected() ? 1L : 0L);
        stmt.bindLong(9, entity.getOrder());
    }

    /**
     * @inheritdoc
     */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }

    /**
     * @inheritdoc
     */
    @Override
    public MyBasket readEntity(Cursor cursor, int offset) {
        MyBasket entity = new MyBasket( //
                cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
                cursor.getInt(offset + 1), // section
                cursor.getInt(offset + 2), // type
                cursor.getString(offset + 3), // name
                cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // value
                cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // categoryId
                cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // categoryName
                cursor.getShort(offset + 7) != 0, // selected
                cursor.getInt(offset + 8) // order
        );
        return entity;
    }

    /**
     * @inheritdoc
     */
    @Override
    public void readEntity(Cursor cursor, MyBasket entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSection(cursor.getInt(offset + 1));
        entity.setType(cursor.getInt(offset + 2));
        entity.setName(cursor.getString(offset + 3));
        entity.setValue(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setCategoryId(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setCategoryName(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setSelected(cursor.getShort(offset + 7) != 0);
        entity.setOrder(cursor.getInt(offset + 8));
    }

    /**
     * @inheritdoc
     */
    @Override
    protected Long updateKeyAfterInsert(MyBasket entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }

    /**
     * @inheritdoc
     */
    @Override
    public Long getKey(MyBasket entity) {
        if (entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /**
     * @inheritdoc
     */
    @Override
    protected boolean isEntityUpdateable() {
        return true;
    }

}
